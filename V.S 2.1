import React, { useState } from 'react';
import { View, Text, TouchableOpacity, StyleSheet, SafeAreaView, Image, ScrollView } from 'react-native';

const App = () => {
  const [currentScreen, setCurrentScreen] = useState('login'); // 'login', 'produtos', 'carrinho'

  // Dados de exemplo
  const produtos = [
    { id: 1, nome: 'Paracetamol', preco: 'R$ 12,90', imagem: 'https://via.placeholder.com/150/FFB6C1/800080?text=Remedio' },
    { id: 2, nome: 'Dipirona', preco: 'R$ 8,50', imagem: 'https://via.placeholder.com/150/FFB6C1/800080?text=Remedio' },
    { id: 3, nome: 'Ibuprofeno', preco: 'R$ 15,75', imagem: 'https://via.placeholder.com/150/FFB6C1/800080?text=Remedio' },
  ];

  const renderScreen = () => {
    switch (currentScreen) {
      case 'login':
        return (
          <View style={styles.screenContainer}>
            <View style={styles.logoCircle}>
              <Text style={styles.logoText1}>DROGARIA</Text>
              <Text style={styles.logoText2}>CESAR</Text>
            </View>

            <TouchableOpacity 
              style={styles.button} 
              onPress={() => setCurrentScreen('produtos')}>
              <Text style={styles.buttonText}>login</Text>
            </TouchableOpacity>

            <Text style={styles.signupText}>não tem uma conta?</Text>

            <TouchableOpacity style={styles.button}>
              <Text style={styles.buttonText}>criar</Text>
            </TouchableOpacity>
          </View>
        );

      case 'produtos':
        return (
          <View style={styles.screenContainer}>
            <Text style={styles.title}>Nossos Produtos</Text>
            
            <ScrollView>
              {produtos.map(produto => (
                <TouchableOpacity 
                  key={produto.id} 
                  style={styles.produtoCard}
                  onPress={() => setCurrentScreen('carrinho')}>
                  <Image 
                    source={{ uri: produto.imagem }} 
                    style={styles.produtoImage} 
                  />
                  <View style={styles.produtoInfo}>
                    <Text style={styles.produtoNome}>{produto.nome}</Text>
                    <Text style={styles.produtoPreco}>{produto.preco}</Text>
                  </View>
                </TouchableOpacity>
              ))}
            </ScrollView>

            <TouchableOpacity 
              style={styles.backButton} 
              onPress={() => setCurrentScreen('login')}>
              <Text style={styles.buttonText}>Voltar</Text>
            </TouchableOpacity>
          </View>
        );

      case 'carrinho':
        return (
          <View style={styles.screenContainer}>
            <Text style={styles.title}>Seu Carrinho</Text>
            
            <View style={styles.carrinhoItem}>
              <Text style={styles.produtoNome}>Paracetamol</Text>
              <Text style={styles.produtoPreco}>R$ 12,90</Text>
            </View>

            <View style={styles.totalContainer}>
              <Text style={styles.totalText}>Total: R$ 12,90</Text>
            </View>

            <TouchableOpacity style={styles.button}>
              <Text style={styles.buttonText}>Finalizar Compra</Text>
            </TouchableOpacity>

            <TouchableOpacity 
              style={styles.backButton} 
              onPress={() => setCurrentScreen('produtos')}>
              <Text style={styles.buttonText}>Voltar</Text>
            </TouchableOpacity>
          </View>
        );
    }
  };

  return (
    <SafeAreaView style={styles.container}>
      {renderScreen()}
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#FF69B4',
  },
  screenContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 20,
  },
  logoCircle: {
    width: 200,
    height: 200,
    borderRadius: 100,
    backgroundColor: '#FFB6C1',
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 40,
    borderWidth: 3,
    borderColor: '#FFFFFF',
  },
  logoText1: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#800080',
  },
  logoText2: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#800080',
  },
  button: {
    backgroundColor: '#FF1493',
    paddingVertical: 15,
    paddingHorizontal: 50,
    borderRadius: 30,
    marginBottom: 15,
    width: '80%',
    alignSelf: 'center',
  },
  backButton: {
    backgroundColor: '#800080',
    paddingVertical: 12,
    paddingHorizontal: 30,
    borderRadius: 25,
    marginTop: 15,
  },
  buttonText: {
    color: 'white',
    fontSize: 18,
    fontWeight: 'bold',
    textAlign: 'center',
  },
  signupText: {
    color: 'white',
    fontSize: 16,
    marginBottom: 15,
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    color: 'white',
    marginBottom: 30,
  },
  produtoCard: {
    flexDirection: 'row',
    backgroundColor: 'white',
    borderRadius: 15,
    padding: 15,
    marginBottom: 15,
    width: '100%',
    alignItems: 'center',
  },
  produtoImage: {
    width: 60,
    height: 60,
    borderRadius: 30,
    marginRight: 15,
    backgroundColor: '#FFB6C1',
  },
  produtoInfo: {
    flex: 1,
  },
  produtoNome: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#800080',
  },
  produtoPreco: {
    fontSize: 16,
    color: '#FF1493',
  },
  carrinhoItem: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    backgroundColor: 'white',
    borderRadius: 10,
    padding: 15,
    marginBottom: 10,
    width: '100%',
  },
  totalContainer: {
    borderTopWidth: 1,
    borderTopColor: 'white',
    paddingTop: 15,
    marginTop: 15,
    width: '100%',
  },
  totalText: {
    fontSize: 20,
    fontWeight: 'bold',
    color: 'white',
    textAlign: 'center',
  },
});

export default App;
