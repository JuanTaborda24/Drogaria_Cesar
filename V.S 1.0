import React from "react";
import { StatusBar, SafeAreaView, StyleSheet, Text, View, TouchableOpacity } from "react-native";

export default function App() {
  return (
    <SafeAreaView style={styles.container}>
      <StatusBar />
      <LoginScreen />
    </SafeAreaView>
  );
}

function LoginScreen() {
  return (
    <View style={styles.loginContainer}>
      {/* Cabeçalho Drogaria Cesar dentro de uma bola rosa */}
      <View style={styles.logoCircle}>
        <Text style={styles.header1}>DROGARIA</Text>
        <Text style={styles.header2}>CESAR</Text>
      </View>

      {/* Botão Login */}
      <TouchableOpacity style={styles.actionButton}>
        <Text style={styles.actionButtonText}>login</Text>
      </TouchableOpacity>

      {/* Mensagem "não tem uma conta?" */}
      <Text style={styles.accountMessage}>não tem uma conta?</Text>

      {/* Botão Criar */}
      <TouchableOpacity style={styles.actionButton}>
        <Text style={styles.actionButtonText}>criar</Text>
      </TouchableOpacity>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#FF69B4", // Rosa mais escuro
  },
  loginContainer: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    padding: 16,
  },
  logoCircle: {
    backgroundColor: "#FFB6C1", // Rosa claro para o círculo
    width: 200,
    height: 200,
    borderRadius: 100,
    justifyContent: "center",
    alignItems: "center",
    marginBottom: 40,
    borderWidth: 3,
    borderColor: "#FFFFFF",
  },
  header1: {
    fontSize: 24,
    fontWeight: "bold",
    color: "#800080", // Roxo escuro
  },
  header2: {
    fontSize: 24,
    fontWeight: "bold",
    color: "#800080", // Roxo escuro
  },
  actionButton: {
    backgroundColor: "#FF1493", // Rosa profundo para os botões
    paddingHorizontal: 32,
    paddingVertical: 15,
    borderRadius: 30,
    marginBottom: 16,
    width: 200,
    alignItems: 'center',
  },
  actionButtonText: {
    fontSize: 18,
    color: "#FFFFFF",
    fontWeight: 'bold',
  },
  accountMessage: {
    fontSize: 16,
    color: "#FFFFFF",
    marginBottom: 16,
    textShadowColor: 'rgba(0, 0, 0, 0.3)',
    textShadowOffset: {width: 1, height: 1},
    textShadowRadius: 2,
  },
});
